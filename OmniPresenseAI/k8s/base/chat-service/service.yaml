apiVersion: v1
kind: Service
metadata:
  name: chat-service
  namespace: omni-ai
  labels:
    app: chat-service
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: chat-service
