## Python test dependencies for Lambda unit tests
## Install: pip install -r requirements-test.txt

# AWS SDK (must match Lambda runtime version)
boto3>=1.34.0
botocore>=1.34.0

# Testing framework
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0

# AWS mocking (optional — tests use unittest.mock instead)
moto[dynamodb,sns,ce]>=5.0.0

# Code quality
ruff>=0.4.0

# Test result reporting
junitparser>=3.1.0
