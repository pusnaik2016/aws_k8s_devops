apiVersion: v1
kind: Service
metadata:
  name: transaction-ingestion
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "transaction-ingestion.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "transaction-ingestion.selectorLabels" . | nindent 4 }}
